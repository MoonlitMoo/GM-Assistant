<section class="record-shell form-shell">
  <div class="record-panel form-panel">
    <div class="form-panel__header">
      <p class="record-kicker">Campaign</p>
      <h1 class="record-title"><%= @campaign.persisted? ? "Edit Campaign" : "New Campaign" %></h1>
    </div>

    <%= form_with model: @campaign, class: "resource-form" do |form| %>
      <%= form_return_to_field %>

      <% if @campaign.errors.any? %>
        <div class="form-errors">
          <h2><%= pluralize(@campaign.errors.count, "error") %> prevented this campaign from being saved:</h2>
          <ul>
            <% @campaign.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="resource-form__field">
        <%= form.label :name %>
        <%= form.text_field :name, class: "resource-form__input", autofocus: true %>
      </div>

      <div class="resource-form__field">
        <%= form.label :description %>
        <%= form.text_area :description, class: "resource-form__input resource-form__textarea", rows: 6 %>
      </div>

      <div class="resource-form__actions">
        <%= form.submit(@campaign.persisted? ? "Save Campaign" : "Create Campaign",
                        class: "fantasy-button fantasy-button--primary") %>
        <%= link_to "Cancel", cancel_path, class: "resource-form__cancel" %>
      </div>
    <% end %>
  </div>
</section>
