<%= content_body_frame do %>
  <%= render "form",
             form_model: @folder,
             form_url: folder_path(@folder.id),
             cancel_path: form_cancel_path(folder_path(@folder.id)) %>
<% end %>
