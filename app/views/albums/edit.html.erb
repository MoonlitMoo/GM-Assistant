<%= content_body_frame do %>
  <%= render "form",
             form_model: @album,
             form_url: album_path(@album.id),
             cancel_path: form_cancel_path(album_path(@album.id)) %>
<% end %>
